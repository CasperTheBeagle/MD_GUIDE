# 💾 คู่มือการสำรองและกู้คืนข้อมูล

## 🎯 วัตถุประสงค์
เอกสารนี้ให้คำแนะนำในการสำรองและกู้คืนข้อมูลสำหรับ React applications เพื่อให้มั่นใจว่าข้อมูลปลอดภัยและสามารถกู้คืนได้ในกรณีเกิดปัญหา

## 📋 สารบัญ
- [กลยุทธ์การสำรองข้อมูล](#-กลยุทธ์การสำรองข้อมูล)
- [การสำรองข้อมูลอัตโนมัติ](#-การสำรองข้อมูลอัตโนมัติ)
- [การกู้คืนข้อมูล](#-การกู้คืนข้อมูล)
- [การทดสอบการสำรองและกู้คืน](#-การทดสอบการสำรองและกู้คืน)
- [แผนฉุกเฉิน](#-แผนฉุกเฉิน)

## 🎯 กลยุทธ์การสำรองข้อมูล

### 1. หลักการ 3-2-1
- **3 สำเนา**: มีข้อมูลอย่างน้อย 3 สำเนา
- **2 สื่อ**: เก็บข้อมูลใน 2 สื่อที่แตกต่างกัน
- **1 สำเนานอกสถานที่**: มี 1 สำเนาที่เก็บนอกสถานที่

### 2. ประเภทการสำรองข้อมูล
- **Full Backup**: สำรองข้อมูลทั้งหมด
- **Incremental Backup**: สำรองเฉพาะข้อมูลที่เปลี่ยนแปลง
- **Differential Backup**: สำรองข้อมูลที่เปลี่ยนแปลงตั้งแต่ full backup ล่าสุด

### 3. ตารางการสำรองข้อมูล
| ประเภทข้อมูล | ความถี่ | ระยะเวลาเก็บ | วิธีการ |
|-------------|---------|-------------|----------|
| ฐานข้อมูล | รายวัน | 30 วัน | Full + Incremental |
| ไฟล์ผู้ใช้ | รายวัน | 90 วัน | Incremental |
| ไฟล์ระบบ | รายสัปดาห์ | 4 สัปดาห์ | Full |
| การตั้งค่า | รายเดือน | 12 เดือน | Full |

## 🤖 การสำรองข้อมูลอัตโนมัติ

### 1. Database Backup Script
```bash
#!/bin/bash
# database_backup.sh

# ตั้งค่าตัวแปร
DB_NAME="your_database"
DB_USER="your_username"
DB_PASSWORD="your_password"
BACKUP_DIR="/var/backups/database"
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="$BACKUP_DIR/${DB_NAME}_backup_$DATE.sql"

# สร้างไดเรกทอรีสำรองข้อมูล
mkdir -p $BACKUP_DIR

# สำรองข้อมูลฐานข้อมูล
mysqldump -u $DB_USER -p$DB_PASSWORD $DB_NAME > $BACKUP_FILE

# บีบอัดไฟล์สำรองข้อมูล
gzip $BACKUP_FILE

# ลบไฟล์สำรองข้อมูลเก่า (เกิน 30 วัน)
find $BACKUP_DIR -name "*.sql.gz" -mtime +30 -delete

# ส่งไปยัง cloud storage
aws s3 cp $BACKUP_FILE.gz s3://your-backup-bucket/database/

echo "Database backup completed: $BACKUP_FILE.gz"
```

### 2. File Backup Script
```bash
#!/bin/bash
# file_backup.sh

SOURCE_DIR="/var/www/html/uploads"
BACKUP_DIR="/var/backups/files"
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="$BACKUP_DIR/files_backup_$DATE.tar.gz"

# สร้างไดเรกทอรีสำรองข้อมูล
mkdir -p $BACKUP_DIR

# สำรองข้อมูลไฟล์
tar -czf $BACKUP_FILE $SOURCE_DIR

# ลบไฟล์สำรองข้อมูลเก่า (เกิน 7 วัน)
find $BACKUP_DIR -name "*.tar.gz" -mtime +7 -delete

# ส่งไปยัง cloud storage
aws s3 cp $BACKUP_FILE s3://your-backup-bucket/files/

echo "File backup completed: $BACKUP_FILE"
```

### 3. Configuration Backup Script
```bash
#!/bin/bash
# config_backup.sh

CONFIG_DIR="/etc/your-app"
BACKUP_DIR="/var/backups/config"
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="$BACKUP_DIR/config_backup_$DATE.tar.gz"

# สร้างไดเรกทอรีสำรองข้อมูล
mkdir -p $BACKUP_DIR

# สำรองข้อมูลการตั้งค่า
tar -czf $BACKUP_FILE $CONFIG_DIR

# สำรองข้อมูล environment variables
env > $BACKUP_DIR/env_$DATE.txt

# ส่งไปยัง cloud storage
aws s3 cp $BACKUP_FILE s3://your-backup-bucket/config/
aws s3 cp $BACKUP_DIR/env_$DATE.txt s3://your-backup-bucket/config/

echo "Configuration backup completed: $BACKUP_FILE"
```

### 4. Cron Jobs Setup
```bash
# ตั้งค่า cron jobs
crontab -e

# เพิ่มบรรทัดเหล่านี้
# สำรองข้อมูลฐานข้อมูลทุกวันเวลา 02:00
0 2 * * * /path/to/database_backup.sh

# สำรองข้อมูลไฟล์ทุกวันเวลา 03:00
0 3 * * * /path/to/file_backup.sh

# สำรองข้อมูลการตั้งค่าทุกวันอาทิตย์เวลา 04:00
0 4 * * 0 /path/to/config_backup.sh
```

## 🔄 การกู้คืนข้อมูล

### 1. Database Recovery
```bash
#!/bin/bash
# database_recovery.sh

BACKUP_FILE=$1
DB_NAME="your_database"
DB_USER="your_username"
DB_PASSWORD="your_password"

if [ -z "$BACKUP_FILE" ]; then
    echo "Usage: $0 <backup_file>"
    exit 1
fi

# ดาวน์โหลดไฟล์สำรองข้อมูลจาก cloud storage
aws s3 cp s3://your-backup-bucket/database/$BACKUP_FILE /tmp/

# คลายบีบอัดไฟล์
gunzip /tmp/$BACKUP_FILE

# กู้คืนฐานข้อมูล
mysql -u $DB_USER -p$DB_PASSWORD $DB_NAME < /tmp/${BACKUP_FILE%.gz}

echo "Database recovery completed from: $BACKUP_FILE"
```

### 2. File Recovery
```bash
#!/bin/bash
# file_recovery.sh

BACKUP_FILE=$1
TARGET_DIR="/var/www/html/uploads"

if [ -z "$BACKUP_FILE" ]; then
    echo "Usage: $0 <backup_file>"
    exit 1
fi

# ดาวน์โหลดไฟล์สำรองข้อมูลจาก cloud storage
aws s3 cp s3://your-backup-bucket/files/$BACKUP_FILE /tmp/

# คลายบีบอัดไฟล์
tar -xzf /tmp/$BACKUP_FILE -C /

echo "File recovery completed from: $BACKUP_FILE"
```

### 3. Application Recovery
```bash
#!/bin/bash
# application_recovery.sh

BACKUP_DATE=$1

if [ -z "$BACKUP_DATE" ]; then
    echo "Usage: $0 <backup_date>"
    exit 1
fi

# หยุดแอปพลิเคชัน
systemctl stop your-app

# กู้คืนฐานข้อมูล
./database_recovery.sh database_backup_$BACKUP_DATE.sql.gz

# กู้คืนไฟล์
./file_recovery.sh files_backup_$BACKUP_DATE.tar.gz

# กู้คืนการตั้งค่า
./config_recovery.sh config_backup_$BACKUP_DATE.tar.gz

# เริ่มแอปพลิเคชัน
systemctl start your-app

echo "Application recovery completed for: $BACKUP_DATE"
```

## 🧪 การทดสอบการสำรองและกู้คืน

### 1. Backup Verification Script
```bash
#!/bin/bash
# backup_verification.sh

BACKUP_FILE=$1

if [ -z "$BACKUP_FILE" ]; then
    echo "Usage: $0 <backup_file>"
    exit 1
fi

# ดาวน์โหลดไฟล์สำรองข้อมูล
aws s3 cp s3://your-backup-bucket/database/$BACKUP_FILE /tmp/

# คลายบีบอัดไฟล์
gunzip /tmp/$BACKUP_FILE

# ตรวจสอบความสมบูรณ์ของไฟล์
if mysql --check-only -u root -p your_database < /tmp/${BACKUP_FILE%.gz}; then
    echo "Backup verification passed: $BACKUP_FILE"
else
    echo "Backup verification failed: $BACKUP_FILE"
    exit 1
fi
```

### 2. Recovery Testing Script
```bash
#!/bin/bash
# recovery_testing.sh

TEST_DB="test_database"
BACKUP_FILE=$1

if [ -z "$BACKUP_FILE" ]; then
    echo "Usage: $0 <backup_file>"
    exit 1
fi

# สร้างฐานข้อมูลทดสอบ
mysql -u root -p -e "CREATE DATABASE $TEST_DB"

# กู้คืนไปยังฐานข้อมูลทดสอบ
mysql -u root -p $TEST_DB < /tmp/${BACKUP_FILE%.gz}

# ตรวจสอบข้อมูล
TABLE_COUNT=$(mysql -u root -p -e "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema = '$TEST_DB'" | tail -1)
ROW_COUNT=$(mysql -u root -p -e "SELECT SUM(table_rows) FROM information_schema.tables WHERE table_schema = '$TEST_DB'" | tail -1)

echo "Test recovery completed:"
echo "Tables: $TABLE_COUNT"
echo "Rows: $ROW_COUNT"

# ลบฐานข้อมูลทดสอบ
mysql -u root -p -e "DROP DATABASE $TEST_DB"
```

### 3. Automated Testing Schedule
```bash
# เพิ่มใน crontab
# ทดสอบการสำรองข้อมูลทุกวันเวลา 05:00
0 5 * * * /path/to/backup_verification.sh database_backup_$(date +\%Y\%m\%d).sql.gz

# ทดสอบการกู้คืนทุกวันอาทิตย์เวลา 06:00
0 6 * * 0 /path/to/recovery_testing.sh database_backup_$(date +\%Y\%m\%d).sql.gz
```

## 🚨 แผนฉุกเฉิน

### 1. ระดับฉุกเฉิน
- **ระดับ 1 (ฉุกเฉินสูงสุด)**: ระบบล้มเหลวทั้งหมด
- **ระดับ 2 (ฉุกเฉัน)**: ฐานข้อมูลเสียหาย
- **ระดับ 3 (ด่วน)**: ไฟล์สำคัญเสียหาย
- **ระดับ 4 (ปานกลาง)**: ข้อมูลบางส่วนเสียหาย

### 2. ขั้นตอนการตอบสนองฉุกเฉัน
1. **ประเมินสถานการณ์**: ระบุปัญหาและผลกระทบ
2. **แจ้งผู้ที่เกี่ยวข้อง**: แจ้งทีมและผู้ใช้
3. **เริ่มการกู้คืน**: ดำเนินการกู้คืนข้อมูล
4. **ตรวจสอบ**: ตรวจสอบว่าการกู้คืนสำเร็จ
5. **ทบทวน**: ทบทวนและปรับปรุงกระบวนการ

### 3. Contact List
- **หัวหน้าทีม**: +66-2-123-4567
- **ฐานข้อมูล**: +66-2-123-4568
- **ระบบเครือข่าย**: +66-2-123-4569
- **คลาวด์**: +66-2-123-4570

### 4. Emergency Commands
```bash
# หยุดระบบทันที
systemctl stop your-app

# กู้คืนจาก backup ล่าสุด
./application_recovery.sh $(date +%Y%m%d)

# ตรวจสอบสถานะระบบ
systemctl status your-app

# ตรวจสอบ logs
tail -f /var/log/your-app/error.log
```

## 📊 รายงานและการติดตาม

### 1. Backup Status Report
```bash
#!/bin/bash
# backup_status_report.sh

echo "=== Backup Status Report ==="
echo "Date: $(date)"
echo ""

# Database backup status
echo "Database Backups:"
aws s3 ls s3://your-backup-bucket/database/ | tail -5
echo ""

# File backup status
echo "File Backups:"
aws s3 ls s3://your-backup-bucket/files/ | tail -5
echo ""

# Config backup status
echo "Configuration Backups:"
aws s3 ls s3://your-backup-bucket/config/ | tail -5
echo ""

# Disk usage
echo "Disk Usage:"
df -h | grep -E "(/$|/var)"
```

### 2. Backup Metrics
- **Success Rate**: อัตราความสำเร็จของการสำรองข้อมูล
- **Backup Size**: ขนาดไฟล์สำรองข้อมูล
- **Backup Duration**: ระยะเวลาการสำรองข้อมูล
- **Recovery Time**: ระยะเวลาการกู้คืน
- **Data Integrity**: ความสมบูรณ์ของข้อมูล

## 📝 รายการตรวจสอบ (Checklist)

### รายวัน
- [ ] ตรวจสอบว่า backup ทำงานได้
- [ ] ตรวจสอบขนาดไฟล์ backup
- [ ] ตรวจสอบพื้นที่จัดเก็บ
- [ ] ตรวจสอบ logs ของ backup

### รายสัปดาห์
- [ ] ทดสอบการกู้คืน
- [ ] ตรวจสอบความสมบูรณ์ของ backup
- [ ] ทบทวนนโยบายการสำรองข้อมูล
- [ ] อัปเดต scripts การสำรองข้อมูล

### รายเดือน
- [ ] ทดสอบการกู้คืนแบบเต็มรูปแบบ
- [ ] ตรวจสอบการเก็บข้อมูลนอกสถานที่
- [ ] ทบทวนแผนฉุกเฉิน
- [ ] ฝึกซ้อมทีมการตอบสนองฉุกเฉัน

---

*เอกสารนี้อัปเดตล่าสุด: 20 มกราคม 2026*  
*เวอร์ชัน: 1.0*
