# 🤝 การมีส่วนร่วมในการพัฒนา [Project Name]

ขอบคุณที่สนใจมีส่วนร่วมในการพัฒนาโปรเจคนี้! เอกสารนี้จะแนะนำวิธีการมีส่วนร่วมต่างๆ

## 📋 สารบัญ
- [🎯 วิธีการมีส่วนร่วม](#-วิธีการมีส่วนร่วม)
- [🚀 การเริ่มต้น](#-การเริ่มต้น)
- [📝 กระบวนการทำงาน](#-กระบวนการทำงาน)
- [🎨 มาตรฐานการเขียนโค้ด](#-มาตรฐานการเขียนโค้ด)
- [🧪 การทดสอบ](#-การทดสอบ)
- [📋 ประเภทการมีส่วนร่วม](#-ประเภทการมีส่วนร่วม)
- [⚖️ ใบอนุญาต](#️-ใบอนุญาต)

## 🎯 วิธีการมีส่วนร่วม

### 1. รายงาน Issues
- 🐛 **Bugs**: รายงานปัญหาที่พบ
- ✨ **Features**: เสนอฟีเจอร์ใหม่
- 📚 **Documentation**: แจ้งปัญหาเกี่ยวกับเอกสาร
- 🎨 **UI/UX**: เสนอการปรับปรุงหน้าตา

### 2. ส่ง Pull Requests
- 🛠️ แก้ไข bugs
- ✨ เพิ่มฟีเจอร์ใหม่
- 📚 ปรับปรุง documentation
- 🧪 เพิ่ม tests
- 🎨 ปรับปรุง UI/UX

### 3. การมีส่วนร่วมอื่นๆ
- 💬 ตอบคำถามใน issues
- 📖 อ่านและ review pull requests
- 🌍 แปลเอกสาร
- 📢 แชร์โปรเจค

## 🚀 การเริ่มต้น

### Prerequisites
- Node.js 18+
- npm 8+ หรือ yarn 1.22+
- Git 2.30+
- ความรู้พื้นฐาน React/TypeScript

### การติดตั้ง
```bash
# 1. Fork repository
# คลิกปุ่ม "Fork" บน GitHub

# 2. Clone repository ของคุณ
git clone https://github.com/YOUR_USERNAME/project-name.git
cd project-name

# 3. Install dependencies
npm install

# 4. Setup environment
cp .env.example .env
# แก้ไข .env ตามความเหมาะสม

# 5. เริ่ม development server
npm run dev
```

### การตั้งค่า Development Environment
```bash
# ติดตั้ง pre-commit hooks
npm run prepare

# ติดตั้ง VS Code extensions (แนะนำ)
code --install-extension ms-vscode.vscode-typescript-next
code --install-extension esbenp.prettier-vscode
code --install-extension ms-vscode.vscode-eslint
```

## 📝 กระบวนการทำงาน

### 1. สร้าง Branch
```bash
# อัปเดต main branch
git checkout main
git pull upstream main

# สร้าง branch ใหม่
git checkout -b feature/your-feature-name

# หรือสำหรับ bug fixes
git checkout -b bugfix/issue-description
```

### 2. การพัฒนา
```bash
# รัน development server
npm run dev

# รัน tests ใน background
npm run test:watch

# ตรวจสอบ linting
npm run lint

# จัดรูปแบบโค้ด
npm run format
```

### 3. การทดสอบ
```bash
# รัน tests ทั้งหมด
npm test

# รัน tests พร้อม coverage
npm run test:coverage

# ตรวจสอบ types
npm run type-check
```

### 4. การ Commit
```bash
# ตรวจสอบก่อน commit
npm run pre-commit

# Commit โค้ด
git add .
git commit -m "feat(component): add new user profile component"

# Push ไปยัง fork ของคุณ
git push origin feature/your-feature-name
```

### 5. สร้าง Pull Request
1. เปิด repository ของคุณบน GitHub
2. คลิก "New Pull Request"
3. เลือก branch ที่เพิ่งสร้าง
4. กรอกรายละเอียด PR ตาม template
5. รอ review จากทีม

## 🎨 มาตรฐานการเขียนโค้ด

### 1. การตั้งชื่อ
- **Components**: `PascalCase.tsx`
- **Files**: `camelCase.ts`
- **Constants**: `UPPER_SNAKE_CASE`
- **Directories**: `kebab-case`

### 2. การจัดรูปแบบ
- ใช้ Prettier สำหรับ formatting
- ใช้ ESLint สำหรับ linting
- จำกัดความยาวบรรทัดไม่เกิน 80 characters

### 3. TypeScript
- ใช้ types ทั้งหมด
- หลีกเลี่ยง `any`
- ใช้ interfaces สำหรับ object shapes

### 4. Components
```typescript
// ✅ ดี
interface UserProfileProps {
  user: User;
  onUpdate: (user: User) => void;
}

export const UserProfile: React.FC<UserProfileProps> = ({
  user,
  onUpdate,
}) => {
  // component logic
  return <div>{user.name}</div>;
};

// ❌ ไม่ดี
export const UserProfile = ({ user, onUpdate }) => {
  return <div>{user.name}</div>;
};
```

## 🧪 การทดสอบ

### 1. Unit Tests
```typescript
// tests/components/Button.test.tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { Button } from '@/components/ui/Button';

describe('Button', () => {
  it('renders correctly', () => {
    render(<Button>Click me</Button>);
    expect(screen.getByText('Click me')).toBeInTheDocument();
  });

  it('calls onClick when clicked', () => {
    const handleClick = jest.fn();
    render(<Button onClick={handleClick}>Click me</Button>);
    
    fireEvent.click(screen.getByText('Click me'));
    expect(handleClick).toHaveBeenCalledTimes(1);
  });
});
```

### 2. Integration Tests
- ทดสอบการทำงานร่วมกันของ components
- ทดสอบ API integration
- ทดสอบ user flows

### 3. E2E Tests
- ทดสอบการทำงานของระบบทั้งหมด
- ใช้ Cypress หรือ Playwright

## 📋 ประเภทการมีส่วนร่วม

### 🐛 Bug Reports
เมื่อรายงาน bug กรุณาระบุ:
- **Title**: สรุปปัญหาสั้นๆ
- **Description**: รายละเอียดปัญหา
- **Steps to reproduce**: ขั้นตอนการทำซ้ำ
- **Expected behavior**: ผลลัพธ์ที่คาดหวัง
- **Actual behavior**: ผลลัพธ์ที่เกิดขึ้นจริง
- **Environment**: ข้อมูลสภาพแวดล้อม
- **Screenshots**: ภาพถ่าย (ถ้ามี)

### ✨ Feature Requests
เมื่อเสนอฟีเจอร์ใหม่กรุณาระบุ:
- **Problem**: ปัญหาที่ต้องการแก้ไข
- **Solution**: วิธีแก้ไขที่เสนอ
- **Alternatives**: ทางเลือกอื่นที่พิจารณา
- **Use cases**: สถานการณ์การใช้งาน

### 📚 Documentation
- แก้ไขข้อผิดพลาดในเอกสาร
- เพิ่มตัวอย่างการใช้งาน
- แปลเอกสารเป็นภาษาอื่น
- ปรับปรุงความชัดเจนของเอกสาร

### 🎨 UI/UX Improvements
- ปรับปรุงการใช้งาน
- แก้ไขปัญหา accessibility
- ปรับปรุง responsive design
- เพิ่ม animations หรือ transitions

## 📝 Pull Request Template

```markdown
## 📋 Description
[คำอธิบายสั้นๆ ว่า PR นี้ทำอะไร]

## 🎯 Type of Change
- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## 🧪 Testing
- [ ] Unit tests pass
- [ ] Integration tests pass
- [ ] Manual testing completed

## 📸 Screenshots
[เพิ่มภาพถ่ายถ้าจำเป็น]

## ✅ Checklist
- [ ] Code follows project style guidelines
- [ ] Self-review completed
- [ ] Documentation updated
- [ ] Tests added/updated
```

## ⚖️ ใบอนุญาต

ด้วยการมีส่วนร่วม คุณยอมรับว่าส่วนขยายของคุณจะถูกจัดจำหน่ายภายใต้ใบอนุญาตเดียวกับโปรเจคนี้

## 🏆 การยอมรับ

ผู้มีส่วนร่วบจะได้รับการยอมรับใน:
- **CONTRIBUTORS.md** - รายชื่อผู้มีส่วนร่วม
- **Release notes** - การขอบคุณในแต่ละ release
- **README.md** - ผู้มีส่วนร่วมหลัก

## 📞 ติดต่อ

หากมีคำถามเกี่ยวกับการมีส่วนร่วม:
- **Slack**: #[contributing]
- **Email**: contribute@project.com
- **GitHub Issues**: สร้าง issue พร้อม label `question`

## 🌟 แนวทางพฤติกรรม

### ✨ สิ่งที่ควรทำ
- เป็นมิตรและใจดี
- ให้คำปรึกษาที่เป็นประโยชน์
- ยอมรับความคิดเห็นของผู้อื่น
- ทำตามมาตรฐานโปรเจค
- เขียน tests ให้ครบถ้วน

### ❌ สิ่งที่ไม่ควรทำ
- ใช้คำหยาบหรือไม่สุภาพ
- แก้ไขโค้ดโดยไม่บอกกล่าว
- ส่ง PR ที่ใหญ่เกินไป (แบ่งเป็นส่วนเล็กๆ)
- ละเลย tests หรือ documentation
- ใช้ force push ไปยัง main branch

---

ขอบคุณที่มีส่วนร่วมในการพัฒนา [Project Name]! 🎉

*เอกสารนี้อัปเดตล่าสุด: [วันที่]*
